#include <stdio.h>

/**
 * main - print all single digit numbers startung from 0
 *
 * Return: 0 if successful
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d\n", i);
		i++;
	}
	return (0);
}
