#include <stdio.h>

/**
 * main - print all single digit numbers startung from 0
 *
 * Return: 0 if successful
 */

int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
